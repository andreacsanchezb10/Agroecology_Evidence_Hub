2026-07-30 — **Golden rule 1 was wrong for everyone but Lolita, and so was the script.** The rule read "scripts write to `C:/Users/mlolita/Downloads/`" — a hardcoded personal path presented as a project-wide rule, which for any other team member either fails or points at a profile they don't have. Reworded everywhere to **the running user's own `Downloads/`** (`%USERPROFILE%/Downloads`), with "resolve it, never hardcode a person's path" stated explicitly.
**Fixed `era_harmonize.R` to match**, since a documented-vs-actual mismatch is itself a bug: `USER_HOME` now derives from `Sys.getenv("USERPROFILE")` (backslashes normalised to forward slashes), and `DOWNLOADS` / `CACHE_DIR` / `OUT_DIR` derive from it. The two remaining absolute OneDrive paths (the `03f. Outcome_Codes.tab` outcome ontology and `livestock_ss_overrides.csv`) now come from a new **`HUB_ROOT`** that probes both Hub folder spellings — Lolita's "Knowledge" and Andrea's "Evidence" — and errors clearly if neither exists. The script therefore runs unmodified on any team member's machine.
**Verified by evaluating the real script's own config block** (not a copy): `OUT_DIR`, `CACHE_DIR` and `HUB_ROOT` resolve **byte-identical** to the three values they replaced, all three resolved input files exist, `VERSION_TAG` untouched, `PARSE OK`, and no `Users/mlolita` literal remains anywhere in the script. **No version bump** — the output is unchanged, so this is a portability fix, not a data release.
**Worth knowing:** `path.expand("~")` on this setup returns `C:/Users/<you>/OneDrive - CGIAR/Documents` — Documents is redirected *into* OneDrive, so using `~` would have written outputs into the synced folder, precisely what rule 1 exists to prevent. Recorded in §1 as a warning with the correct idiom.
**Left unfixed, honestly:** the other ERA scripts (`ERA_load_snapshots.R`, `ERA_Data_format.R`, `ERA_bibliography_extract.R`, `era_agroecology_beta.R`, `era_agroecology_slides.R`, and `previous/`) still hardcode `C:/Users/mlolita/…`, as do Andrea's pipeline scripts with their own root. Noted in §1 rather than changed — out of scope here and each needs its own verification. Also note `era_harmonize.R` lives outside the git repo (`ERA/` is not under `Agroecology_Evidence_Hub/`), so this script change has no git history — only OneDrive version history — `CLAUDE.md` + 3 external `CLAUDE.md`, `09_conventions.md` §1, `04_workflow_and_folders.md`, `06_ontologies.md`, `_status/era.md`, `sources/ERA/00_era_overview.md`, `sources/ERA/01_era_harmonization.md`, `ERA/Script/era_harmonize.R`
