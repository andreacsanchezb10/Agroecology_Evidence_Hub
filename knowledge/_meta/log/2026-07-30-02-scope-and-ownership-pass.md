2026-07-30 — **Scope + ownership pass, ahead of handing the folder to Andrea for Claude testing.** (1) Hub scope corrected to **global, not Africa** — Africa is an ERA property only; documented the second source (the in-progress manual `09_FOMD_*.xlsm` paper extraction, global) in `overview.md` + `00_START_HERE.md`. (2) Ontology rule hardened: the live `01_FOMD_ontologies.xlsx` is **never** modified by script or AI — only suggest additions, or write a *new, differently-named* proposal to `Downloads/`; extended to the other FOMD workbooks + extractors' files. (3) **`analysis.md` emptied** — Andrea owns the analysis method; assistants must not invent pooling guidance (data *limitations* still go to `open_issues.md`). (4) Living-doc protocol broadened from "changed code" to **"learned anything durable"**, with a worked trigger table and an explicit new-version→update-every-count rule. Also fixed stale v47 refs and recorded that v49 is built but **unreleased** (Andrea's newest openable file is still v47) — CLAUDE.md, 00_START_HERE, overview, ontology, conventions, folder_structure, analysis, _TEMPLATE, MAINTENANCE, sources/ERA/{overview, output_schema, pipeline_and_run}
