2026-07-30 — **Documented the shared-OneDrive collaboration hazards** as `09_conventions.md` §13, after finding that Lolita and Andrea share one synced folder while both using Claude. Three findings: (1) **`knowledge/` is untracked by git** (`?? knowledge/`, not ignored — never committed), so the whole base has no history and a clobbered doc is unrecoverable; (2) **`.git/` sits inside the synced tree**, so two people running git concurrently makes OneDrive replicate git internals mid-write → repo corruption; rule is now **one designated git machine**; (3) whole-file writes + OneDrive last-writer-wins make `01_status.md` and `UPDATE_LOG.md` the collision points, with a quieter **stale-read** risk where a session acts on numbers OneDrive hasn't pulled yet. Added two health checks (conflict-copy scan, `knowledge/` tracking check). Repo was at 0/0 with origin and no conflict copies existed — ahead of the problem. Committing `knowledge/` and flipping `UPDATE_LOG` to append-at-bottom both left pending the user's decision — `09_conventions.md`, `_meta/MAINTENANCE.md`
