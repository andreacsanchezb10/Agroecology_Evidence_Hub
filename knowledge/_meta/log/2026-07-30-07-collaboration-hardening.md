2026-07-30 — **Hardened the knowledge base for team use, ahead of several people running Claude against this one shared OneDrive folder.** Five changes, all aimed at the same failure: OneDrive never merges, and an assistant's whole-file write turns a merge conflict into a silently clobbered document.
(1) **`01_status.md` split into six owned files under `_status/`** (`sources`, `era`, `extraction`, `code`, `ontology`, `project`), each naming its owner and carrying its own dated "verified" line; `01_status.md` is now a **thin index you read** — every number still lives in exactly one place, but the file you *write* is no longer the file everyone writes. Rule 8 reworded accordingly; content moved verbatim, no figures changed.
(2) **`_meta/UPDATE_LOG.md` replaced by one file per entry in `_meta/log/`** (`YYYY-MM-DD-NN-slug.md`). The old top-appended list made two simultaneous sessions write the same first line, losing one every time; separate filenames cannot overwrite each other. All 11 prior entries split out unchanged, including the two undated `v46`/`v47` ones. `UPDATE_LOG.md` kept as the convention doc so existing links still resolve.
(3) **Golden rule 2 is now mechanically enforced**, not just written: `.claude/settings.json` at the **Hub root**, in **`Agroecology_Evidence_Hub/`** and in **`ERA/Script/`** `deny` writes to `02.metadata_structure/` (master workbooks + ontology), `03.extraction/`, `01.SOMD/`, `ERA/data/`, `protocol/`, `partners/` and the loose V3 extraction template. Three settings files rather than one because deny patterns anchored at `./` or `**/` resolve **against the project root**, so a session launched in `ERA/Script/` is not covered by a Hub-root rule. **Empirically verified this session:** `**/dir/**` and `./dir/**` both block writes; `**/…` does **not** reach outside the project root; the absolute form `//c/Users/*/…` does, and `*` matches both a user name and a path segment containing spaces — hence the primary rules use `//c/Users/*/*/*/Agroecology_Evidence_Hub/…`, which covers **Andrea's differently-named root** and any future teammate from any cwd. A decoy directory of exactly that shape was refused; the probes were removed afterwards. Limitation recorded: this stops Claude's own edits, **not** an R script's `saveWorkbook()` — the incident behind rule 2 was a script, so rule 2 still binds scripts.
(4) **New rule: surgical edits only, never whole-file rewrites, on anything in `knowledge/`** — a targeted edit *fails loudly* when someone else changed that passage, which is the desired outcome; a whole-file write succeeds silently and destroys their work. Added to `CLAUDE.md`, the three pointer `CLAUDE.md` files, and `09_conventions.md` §13, together with ownership-by-folder and announce-in-chat-before-editing-a-shared-doc.
(5) **`knowledge/` committed to git** (it was untracked, so a clobbered doc was unrecoverable), on the designated git machine only — `.git/` sits inside the synced tree, so concurrent git from two machines corrupts the repo. `.claude/settings.local.json` gitignored as a personal override. Not pushed: the remote belongs to Andrea, so publishing is her call.
§13 rewritten from "here are the hazards, two fixes pending sign-off" to what is now actually in place, with what remains unprotected stated plainly (a teammate's Claude legitimately over-rewriting a doc; R scripts) and the escalation path noted — if the team passes ~3 Claude users, move `knowledge/` out of OneDrive into a repo each person clones so git actually merges. Health checks updated: `_status/` verified-dates, the three settings files still present, log still one-file-per-entry, `knowledge/` still tracked — `01_status.md` (now an index), `_status/*` (new), `_meta/log/*` (new), `_meta/UPDATE_LOG.md`, `_meta/MAINTENANCE.md`, `CLAUDE.md`, `00_START_HERE.md`, `09_conventions.md`, `sources/_TEMPLATE.md`, `sources/ERA/{01,02,04}`, + 3 external CLAUDE.md and 3 `.claude/settings.json`
