2026-07-30 — **Each session now identifies who is working, and the one-git-machine rule enforces itself.** Two hooks added to all three `.claude/settings.json` files (Hub root, `Agroecology_Evidence_Hub/`, `ERA/Script/`), both `shell: powershell` so they work on any Windows machine without needing Git Bash:
(1) **`SessionStart`** reads `%USERNAME%` and opens every session by stating who you are, whether you are the designated git machine, and the shared-folder rules; the same text is passed to Claude as `additionalContext`, so the assistant knows the constraints before its first action. **Detected rather than asked** — a question can be answered wrongly, skipped, or simply never asked, whereas the username is unambiguous (`mlolita` vs `andreasanchez`), which also makes it work in non-interactive sessions.
(2) **`PreToolUse`** on `Bash|PowerShell` **denies any git command when `%USERNAME%` is not `mlolita`**, returning `permissionDecision: "deny"` with an explanation pointing at `09_conventions.md` §13. This closes the gap that rule 5 was previously only *agreed*: a teammate's Claude could corrupt the repo (`.git/` sits inside the synced tree) without ever having read the rule. **Designated machine is Lolita's**, decided by her.
**Verified this session:** the guard denies `git status`, `git -C x log`, chained `… && git push` and bare `git` for a non-owner; allows all of them for `mlolita`; and does not false-positive on `ls digital`, `echo legit stuff`, `cat repo/.git/config` or `Rscript era_harmonize.R` (the regex requires a non-alphanumeric before `git` and whitespace/quote after, so paths and substrings don't trip it). JSON escaping was checked by extracting the stored command back out of each settings file and piping real hook payloads through it, and a temporary sentinel confirmed the hook actually fires on a live tool call — sentinel removed afterwards; all three files re-validated clean. Own git access confirmed unaffected.
To change the designated machine, edit `$owner='mlolita'` in the `hooks` block of all three settings files — deliberately one greppable literal rather than a lookup. Limitation: this governs git run *through Claude*; a person typing git in their own terminal is still on the honour system — `09_conventions.md` §13, `_meta/MAINTENANCE.md`, `CLAUDE.md` + 2 external `CLAUDE.md`, 3 × `.claude/settings.json`
