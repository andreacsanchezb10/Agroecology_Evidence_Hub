2026-07-30 — **Expanded the header comment of `ERA/Script/era_harmonize.R` from a 14-line output list into a full orientation block**, so someone opening the script cold understands what it is for before reading any code. No behavioural change; parse-checked (`PARSE OK`), everything below the header untouched.

The header now carries: the project framing (living global review, evidence recycling, ERA as **one source** with `ss_id = MD_Rosen_24_Effec_Sc`, Africa-only as a property of ERA); the target `10_` shape (one row = one C-vs-T comparison per outcome) and the note that `FOMD`/`to_FOMD` in names is a deliberate namespace/legacy prefix; the two-scripts-two-owners handoff diagram and the **three-versions-in-play** warning; the real input list (the four snapshots with their animal codenames, `livestock_ss_overrides.csv`, `animal_species_by_study.csv`) — `po_paper_resolved.csv` is **not** listed because the v40 layer that read it was removed (line ~1772); the full output list including `product_to_confirm_<v>.csv`, which the old header omitted; a step map naming step 4 as where the pairing keys live; the parse-check-first run recipe; the six binding rules; and the gotchas (helper shadowing, never clean an ID, don't gate on `era_domain`, position-aligned parallel lists, separator scope, honest missingness, deferred C/T orientation).

All content was taken from the knowledge base rather than re-derived, so **no doc needed updating and no numbers were copied into the script** — counts and versions stay in `_status/era.md` per golden rule 4. Docs updated: none (this log entry only).
