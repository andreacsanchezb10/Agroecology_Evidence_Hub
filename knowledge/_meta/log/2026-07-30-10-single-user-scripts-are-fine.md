2026-07-30 — **Clarified the scope of golden rule 1, correcting the framing in entry `2026-07-30-09`.** That entry listed the other ERA scripts (`ERA_load_snapshots.R`, `ERA_Data_format.R`, `ERA_bibliography_extract.R`, `era_agroecology_beta.R`, `era_agroecology_slides.R`, `previous/`) as "left unfixed", which read as an outstanding defect. **Lolita's decision: it isn't one.** Those are hers alone, nobody else runs them, so their hardcoded absolute paths are **deliberate** — don't "fix" them and don't log them as a defect.
Rule 1 is a rule about **what the guidelines may state**, and about **shared or handed-over code**: a guideline must never present one person's profile path as the project's path, because everyone reads the guidelines. `era_harmonize.R` stays per-user-resolved because it is the pipeline the team's output depends on and the one most likely to be handed over; that remains correct.
Recorded in `09_conventions.md` §1 (new "scope of this rule" paragraph naming the single-user scripts explicitly), the "Absolute paths" section (softened from "every script" to "nearly every", noting the deliberate case and the one exception), and `08_effect_sizes_and_analysis.md` (the hardcoded-paths gotcha now says deliberate, not a defect to fix). Earlier entries are left untouched — the log is append-only — `09_conventions.md`, `08_effect_sizes_and_analysis.md`
