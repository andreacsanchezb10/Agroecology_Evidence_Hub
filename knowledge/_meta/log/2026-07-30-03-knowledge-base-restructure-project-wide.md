2026-07-30 — **Restructured the knowledge base from "ERA harmonization" to the whole project.** New flat numbered doc set `00`–`09` + `sources/`: `01_status.md` (**now the sole owner of every number, version and count** — the fix for counts drifting across 7 files), `02_the_project.md` (the living systematic review, ROSES, global scope, the *evidence-recycling* framing, partners, consumers), `03_somd_and_fomd.md`, `04_workflow_and_folders.md` (the 9 stages == the folder tree), `05_data_schemas.md` (the `10_` template + the `06_`/`09_` inputs), `06_ontologies.md`, `07_extraction.md` (the `PI` manual route), `08_effect_sizes_and_analysis.md` (Part A = factual code map, Part B = Andrea's method page, still empty), `09_conventions.md`. ERA collapsed 7 docs → 5 under `sources/ERA/` with all substance preserved and `03_era_changelog.md` byte-identical. **Three corrections:** (1) **FOMD/SOMD terminology fixed** — FOMD = First-Order Meta-Data (unit = a primary study), *not* "the target schema"; the schema is the `10_` template. Drift removed from 6 files, with an explicit carve-out that `FOMD` in filenames/objects is a correct namespace prefix and must never be renamed. (2) **The rules were never loading** — no `CLAUDE.md` existed at the Hub root or in `ERA/Script/`, so `knowledge/CLAUDE.md` only applied when cwd was inside `knowledge/`; added pointer files at both, shrank `Agroecology_Evidence_Hub/CLAUDE.md` to a pointer. (3) **Documented the previously-undocumented ERA handoff** (`sources/ERA/02_era_handoff.md`): `era_harmonize.R` does not feed the effect-size pipeline directly — Andrea's `added_to_10_…R` does, on an older version, from a differently-named OneDrive root. Also verified `04.fomd10_effect_size.R` *does* produce `effect_size_yi` (line 185), correcting a stale claim — all docs, `sources/ERA/*`, MAINTENANCE, UPDATE_LOG, + 3 external CLAUDE.md
