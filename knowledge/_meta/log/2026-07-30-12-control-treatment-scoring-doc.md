2026-07-30 — **Added a new knowledge doc `control_treatment_scoring.md`** capturing the agroecological-gradient reframing of how control and treatment are defined for the Hub's core question, and registered it in the doc map (`CLAUDE.md`).

The doc records the decision to stop inheriting a source dataset's `C_*`/`T_*` orientation and instead re-derive it from practice content, anchored in Wezel et al. (2020) and Gliessman (2007, 2016): the two-dimension score (transition **depth** = deepest Gliessman level reached, a max not a sum; principle **breadth** = number of distinct field-scale principles engaged), the orientation rule (compare only the *differing* families; deeper arm = treatment; breadth as tie-break; else lateral), the L0–L3 practice crosswalk with FAO/HLPE principle anchors, the set-aside buckets (no-contrast / lateral / unscoreable / confounded / conflicting), and the two hypotheses (H1 depth, H2 synergy/breadth). Kept **qualitative** (no counts, per golden rule 4) and flagged throughout as a **draft for Andrea to ratify** (golden rule 5). Cross-links `08_effect_sizes_and_analysis.md`, `sources/ERA/04_era_open_issues.md` (the `ct_reoriented` orientation problem it addresses), and the implementing script `ERA/Script/era_agroecology_beta.R`. A fuller circulatable Word version exists as a separate draft protocol (not in the knowledge base).

Docs updated: new `control_treatment_scoring.md`; one row added to the doc map in `CLAUDE.md`.
